<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".SavingsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.SavingsWidgetProvider"
            android:exported="true"
            android:label="Savings Progress Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.widget.ACTION_SUB_10" />
                <action android:name="com.example.widget.ACTION_ADD_10" />
                <action android:name="com.example.widget.ACTION_ADD_50" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/savings_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.SavingsGridWidgetProvider"
            android:exported="true"
            android:label="Savings Grid Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.widget.GRID_ACTION_SUB_10" />
                <action android:name="com.example.widget.GRID_ACTION_ADD_10" />
                <action android:name="com.example.widget.GRID_ACTION_ADD_50" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/savings_grid_widget_info" />
        </receiver>
    </application>

</manifest>
